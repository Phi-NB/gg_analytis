export enum MESSAGE_SUCCESS {
  GET_RUN_REPORT = "Get data run report success.",
}

export enum MESSAGE_ERROR {
  ERROR_VALIDATE = "Error validate.",
  DATE_INVALID_FORMAT = "Date invalid format.",
  DATE_INVALID = "Date invalid.",
}
